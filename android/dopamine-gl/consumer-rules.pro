# Effects self-register via their generated/registration objects; nothing here
# needs to survive reflection, so no keep rules are required. Placeholder so the
# `consumerProguardFiles` reference resolves.
